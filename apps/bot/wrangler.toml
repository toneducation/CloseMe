name = "closeme-bot"
main = "src/index.ts"
# Preserve runtime variables configured from the owner’s phone.
keep_vars = true
compatibility_date = "2026-09-01"
compatibility_flags = ["nodejs_compat"]
[observability]
enabled = false

[triggers]
crons = ["* * * * *"]
